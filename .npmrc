# Hoisted installs avoid broken resolution of @expo/* and expo-modules-* in this pnpm
# workspace (see https://docs.expo.dev/guides/monorepos/#package-managers-with-isolated-dependencies).
node-linker=hoisted
