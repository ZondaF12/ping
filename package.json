{
  "name": "ping",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "api": "pnpm --filter @ping/api start:dev",
    "mobile": "pnpm --filter @ping/mobile start",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  }
}
